A simple out-of-the-box variable height `UITableView`-based UI of multiple
stack views & some auto layout to create a Twitter-like UI.

## How to Run

```
open -a Xcode scroll-performance-native.xcodeproj
# Click 'Run' in Xcode
```
